# Core dependencies
aiohttp>=3.9
PyJWT>=2.8
aiosqlite>=0.19
python-dotenv>=1.0
argon2-cffi>=23.1
pyotp>=2.9.0
aiofiles>=23.0
asyncssh>=2.14
cryptography>=41.0
psutil>=5.9

# Optional: faster event loop (Linux only, not required)
# uvloop>=0.19; sys_platform != 'win32'
